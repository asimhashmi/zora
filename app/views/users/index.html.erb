<%= search_form_for @q do |f| %>

  # Search if the name field contains...
  <%= f.label :bio_cont %>
  <%= f.search_field :role_cont %>
  <%#= f.select :schedules_county_matches_all, Schedule.counties.map { |r| [r[0], r[1].to_i] }, include_blank: true %>
  <%= f.select :role_eq, User.roles.to_a.map { |w| [w[0].humanize, w[1]] },
                         {:include_blank => true} %>


  <%= f.submit %>
<% end %>


<h1>All Users</h1>
<p class="lead">Use this for your users page</p>

<br>

<h5>List of verified teachers</h5>

    <table class="table table-striped table-inverse table-responsive">
        <thead class="thead-inverse">
            <tr>
                <th>name</th>
                <th>Bio</th>
                <th>Province</th>
            </tr>
            </thead>
            <tbody>
                <% @user.each do |list|%>
                    <tr>
                        <td scope="row"><%= link_to list.name, list %></td>
                        <td><%= list.bio %></td>
                        <td><%= list.province %></td>
                    </tr>
                <% end %>
            </tbody>
    </table>
