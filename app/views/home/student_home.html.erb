<%= search_form_for @q do |f| %>

  # Search if the name field contains...
  <%= f.label :bio_cont %>
  <%= f.search_field :first_name_cont %>
  <%#= f.select :schedules_county_matches_all, Schedule.counties.map { |r| [r[0], r[1].to_i] }, include_blank: true %>
  <%= f.select :role_eq, User.roles.to_a.map { |w| [w[0].humanize, w[1]] },
                         {:include_blank => true} %>


  <%= f.submit %>
<% end %>




<h1>Student</h1>
<p class="lead">Use this for your student home page</p>

<% form_tag student_path, :method => 'get' do %>
  <p>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search", :name => nil %>
  </p>
<% end %>
<br>
<br>

<h5>List of verified teachers</h5>

    <table class="table table-striped table-inverse table-responsive">
        <thead class="thead-inverse">
            <tr>
                <th>name</th>
                <th>Bio</th>
                <th>Province</th>
            </tr>
            </thead>
            <tbody>
                <% @teacher_list.each do |teacher|%>
                    <tr>
                        <td scope="row"><%= link_to teacher.name, user_path(teacher.id) %></td>
                        <td><%= teacher.bio %></td>
                        <td><%= teacher.province %></td>
                    </tr>
                <% end %>
            </tbody>
    </table>
